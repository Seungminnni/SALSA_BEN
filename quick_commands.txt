# SALSA-repro 완전 자동화 실행 명령어들 v2.0
# ================================================

# 🚀 완전 자동화 실행 (최신 권장)
py run_complete.py          # Python 스크립트 직접 실행
.\run_complete.bat          # Windows 배치 파일
.\run_complete.ps1          # PowerShell 스크립트

# 🔧 이전 버전들  
py run_all.py               # 기본 자동화 버전
.\run_all.bat               # 기본 자동화 배치 파일
py run_simple.py            # 간단 버전

# 📋 단계별 수동 실행
py -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
py -m pip install -r requirements.txt
py src/data_gen_obfuscate_fixed.py
py src/run_salsa_connected.py
py src/evaluate_and_plot.py

# 📊 결과 확인
type results\salsa_runs\salsa_summary.csv
notepad results\salsa_runs\salsa_summary.json

# 🔍 GPU 상태 확인
py -c "import torch; print(f'CUDA: {torch.cuda.is_available()}, GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"None\"}')"